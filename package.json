{
  "name": "hunk-herdr",
  "version": "0.1.0",
  "description": "Workspace-scoped Herdr agent picker and prompting for Hunk",
  "type": "module",
  "hunk": { "extensions": ["./index.ts"], "apiVersion": 10 },
  "files": ["index.ts", "bridge.ts", "README.md"],
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "hunkdiff": "0.22.0",
    "typescript": "^5.9.0"
  }
}
